import { DragListItemDirective } from './drag-list-item.directive';

describe('DragListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DragListItemDirective();
    expect(directive).toBeTruthy();
  });
});
