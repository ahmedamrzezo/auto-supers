import { RequiredFileDirective } from './required-file.directive';

describe('RequiredFileDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredFileDirective();
    expect(directive).toBeTruthy();
  });
});
